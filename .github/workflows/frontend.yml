name: frontend

on:
  push:
    paths:
      - 'frontend/**'

jobs:      
  deploy:
    name: deploy
    needs: lint
    runs-on: self-hosted  
    steps:
      - name: Deploy with Docker Compose
        run: |
          cd /home/superfrog/ft/vuz/tkr/repo
          cd frontend
          docker-compose down
          docker-compose up -d --build
